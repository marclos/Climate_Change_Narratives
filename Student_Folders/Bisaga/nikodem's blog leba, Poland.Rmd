---
title: "leba, PL blog"
author: "nikodem bisaga"
date: "9/20/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

file.choose()
"/home/CAMPUS/jnba2020/Climate_Change_Narratives/Student_Folders/Bisaga/leba, poland data.csv"
read.csv("/home/CAMPUS/jnba2020/Climate_Change_Narratives/Student_Folders/Bisaga/leba, poland data.csv")
climate_data <- read.csv("/home/CAMPUS/jnba2020/Climate_Change_Narratives/Student_Folders/Bisaga/leba, poland data.csv")
head(climate_data)
str(climate_data)
names(climate_data)
plot(TMAX~DATE, climate_data)

#climate_data$NewDate <- as.Date(strDates, "%m/%d/%y")

climate_data$NewDate = NA
climate_data$NewDate[1:12418] = format(as.Date(strDates[1:12418], "%m/%d/%y"), "19%y-%m-%d") 
climate_data$NewDate[12419:19949] = format(as.Date(strDates[12419:19949], "%m/%d/%y"),"20%y-%m-%d") 








